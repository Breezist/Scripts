return {
	name = `cmds`,
	description = `Gives a list of commands!`,
	arguments = {},
	aliases = {},
	permissionLevel = 100,
	execution = function(player : Player, args : {})
		print(`Hello world!`)
	end,
}